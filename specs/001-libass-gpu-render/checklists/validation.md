# Validation Artifacts Checklist

- [ ] GPU pipeline playback screenshot/recording with complex ASS sample (SurfaceView)
- [ ] GPU pipeline playback screenshot/recording with complex ASS sample (TextureView)
- [ ] Telemetry overlay capture showing vsync hit rate and dropped frames
- [ ] Log extracts for fallback/recovery (`SUB-FALLBACK`) including surface recreate scenario
- [ ] `SubtitleTelemetryRepository.latestSnapshot` output after ≥20 min soak
- [ ] Note on load-shedding triggers (`LOAD_SHED`) and whether subtitles remained readable
