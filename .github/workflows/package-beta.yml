name: package beta

# 主分支提交代码时，构建最新测试包
on:
  push:
    branches:
      - '**'  # 匹配所有分支
  workflow_dispatch:  # 允许手动触发

jobs:
  package:
    name: Generate Beta APK
    runs-on: ubuntu-latest
    steps:
      # 1.拉取代码
      - name: checkout source code
        uses: actions/checkout@v4

      # 2.配置JDK
      - name: set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: 11
          distribution: adopt
      - name: install Android 35 SDK
        run: |
          set -euo pipefail
          SDKMANAGER="$(command -v sdkmanager || true)"
          if [[ -z "$SDKMANAGER" ]]; then
            SDKMANAGER="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}/cmdline-tools/latest/bin/sdkmanager"
          fi
          if [[ ! -x "$SDKMANAGER" ]]; then
            echo "sdkmanager not found" >&2
            exit 1
          fi
          yes | "$SDKMANAGER" --licenses > /dev/null
          yes | "$SDKMANAGER" "platforms;android-35" "build-tools;35.0.0"

      # 3.注入密钥到环境变量
      - name: inject secrets to environment  
        env:
          BUGLY_APP_ID: ${{ secrets.BUGLY_APP_ID }}
          DANDAN_APP_ID: ${{ secrets.DANDAN_APP_ID }}
          ALIYUN_SECRET: ${{ secrets.ALIYUN_SECRET }}
        run: |
          echo "✅ Secrets injected for beta build"
          echo "BUGLY_APP_ID length: ${#BUGLY_APP_ID}"

      # 4.获取签名密钥
      - name: decrypt key
        run: |
          gpg --quiet --batch --yes --decrypt \
          --passphrase=${{ secrets.KEY_PASSPHRASE }} \
          --output gradle/assemble/dandanplay.jks gradle/assemble/dandanplay.jks.gpg

      # 4.配置构建类型
      - name: setup build type
        run: echo "BUILD_TYPE=beta" >> $GITHUB_ENV

      # 5.获取分支名称
      - name: get branch name
        run: |
          # 提取分支名，移除 refs/heads/ 前缀
          branch_name=${GITHUB_REF#refs/heads/}
          # 将特殊字符替换为 - ，确保tag名称有效
          safe_branch_name=$(echo "$branch_name" | sed 's/[^a-zA-Z0-9._-]/-/g')
          echo "BRANCH_NAME=$safe_branch_name" >> $GITHUB_ENV
          echo "原始分支名: $branch_name"
          echo "安全分支名: $safe_branch_name"

      # 6.构建安装包
      - name: assemble apk
        env:
          # 签名相关密钥
          KEYSTORE_PASS: ${{ secrets.KEYSTORE_PASS }}
          ALIAS_NAME: ${{ secrets.ALIAS_NAME }}
          ALIAS_PASS: ${{ secrets.ALIAS_PASS }}
          # 应用密钥 - 从Secrets注入
          BUGLY_APP_ID: ${{ secrets.BUGLY_APP_ID }}
          DANDAN_APP_ID: ${{ secrets.DANDAN_APP_ID }}
          ALIYUN_SECRET: ${{ secrets.ALIYUN_SECRET }}
        run: chmod +x gradlew &&./gradlew clean assemble${{ env.BUILD_TYPE }} --stacktrace

      # 6.配置构建版本
      - name: setup build version
        run: |
          apk_version=`egrep -o "[0-9].[0-9].[0-9]" buildSrc/src/main/java/Versions.kt`
          echo "BUILD_VERSION=$apk_version" >> $GITHUB_ENV

      # 7.移动文件到根目录
      - name: move file
        run: |
          abis=(arm64-v8a armeabi-v7a universal)
          for abi in ${abis[@]}
          do
            # 原文件名
            original_name="dandanplay_v${{ env.BUILD_VERSION }}_${abi}-${{ env.BUILD_TYPE }}.apk"
            # 新文件名（包含分支信息）
            new_name="dandanplay_v${{ env.BUILD_VERSION }}_${abi}-${{ env.BUILD_TYPE }}-${{ env.BRANCH_NAME }}.apk"
            mv app/build/outputs/apk/${{ env.BUILD_TYPE }}/${original_name} ${new_name}
          done

      # 8.创建Release
      - name: generate release
        id: generate_release
        uses: softprops/action-gh-release@v1
        with:
          name: 弹弹play概念版 v${{ env.BUILD_VERSION }} Beta (${{ env.BRANCH_NAME }})
          tag_name: dandanplay-v${{ env.BUILD_VERSION }}-beta-${{ env.BRANCH_NAME }}
          body: |
            🚀 最新测试包 - 分支: ${{ env.BRANCH_NAME }}
            📅 构建时间: ${{ github.event.head_commit.timestamp }}
            📝 最新提交: ${{ github.event.head_commit.message }}
            👤 提交者: ${{ github.event.head_commit.author.name }}
          prerelease: true
          files: |
            dandanplay_v${{ env.BUILD_VERSION }}_arm64-v8a-${{ env.BUILD_TYPE }}-${{ env.BRANCH_NAME }}.apk
            dandanplay_v${{ env.BUILD_VERSION }}_armeabi-v7a-${{ env.BUILD_TYPE }}-${{ env.BRANCH_NAME }}.apk
            dandanplay_v${{ env.BUILD_VERSION }}_universal-${{ env.BUILD_TYPE }}-${{ env.BRANCH_NAME }}.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
