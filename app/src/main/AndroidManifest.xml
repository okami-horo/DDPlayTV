<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name="com.okamihoro.ddplaytv.app.IApplication"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">

	        <activity
	            android:name="com.okamihoro.ddplaytv.ui.splash.SplashActivity"
	            android:configChanges="orientation|screenSize"
	            android:theme="@style/SplashTheme"
	            android:exported="true">
	            <intent-filter>
	                <action android:name="android.intent.action.MAIN" />

	                <category android:name="android.intent.category.LAUNCHER" />
	            </intent-filter>
	        </activity>

	        <activity
	            android:name="com.okamihoro.ddplaytv.ui.splash.TvSplashActivity"
	            android:configChanges="orientation|screenSize"
	            android:theme="@style/SplashTheme"
	            android:banner="@mipmap/ic_leanback_banner"
	            android:exported="true">
	            <intent-filter>
	                <action android:name="android.intent.action.MAIN" />

	                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
	            </intent-filter>
	        </activity>

	        <activity
	            android:name="com.okamihoro.ddplaytv.ui.main.MainActivity"
	            android:launchMode="singleTask" />

	        <activity
	            android:name="com.okamihoro.ddplaytv.ui.tv.TvMainActivity"
	            android:launchMode="singleTask" />

        <service
            android:name="com.okamihoro.ddplaytv.app.service.Media3SessionService"
            android:exported="false" />
    </application>

</manifest>
